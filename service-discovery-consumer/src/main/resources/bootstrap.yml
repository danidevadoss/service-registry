spring:
  application:
    name: service-discovery-consumer

server:
  port: 8082

producer:
  ribbon:
    ServerListRefreshInterval: 5000
ribbon:
  IsSecure: false
  
management:
  security:
    enabled: false